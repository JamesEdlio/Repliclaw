{
  "name": "repliclaw",
  "version": "0.2.0",
  "description": "Clone-thyself primitive for AI agents: spawn disposable replicas to run scoped task skills in isolation.",
  "type": "module",
  "repository": "github:JamesEdlio/Repliclaw",
  "license": "MIT",
  "scripts": {
    "test": "node tests/run.mjs && node --test tests/parse-result.test.mjs tests/runtimes.test.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
